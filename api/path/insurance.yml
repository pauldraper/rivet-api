/accounts/{accountId}/insurancePlans:
  parameters:
    - $ref: "../parameter/account.yml#/accountId"
  get:
    summary: List insurance plans
    tags:
      - Insurance
    operationId: listInsurancePlans
    responses:
      "200":
        description: Success.
        content:
          application/json:
            schema:
              type: array
              items: { $ref: "../schema/insurance.yml#/insurancePlan" }
