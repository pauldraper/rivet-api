personName:
  description: Person name
  properties:
    first:
      description: First name.
      maxLength: 100
      title: First
      type: string
    last:
      description: Last name.
      maxLength: 100
      title: Last
      type: string
    middle:
      default: ""
      description: Middle name.
      maxLength: 100
      title: Middle
      type: string
    suffix:
      default: ""
      description: Suffix.
      maxLength: 20
      title: Suffix
      type: string
  required: [first, last]
  title: Person name
  type: object
